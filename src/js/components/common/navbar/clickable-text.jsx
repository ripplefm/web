import styled from '@emotion/styled';

export default styled.span`
  font-weight: 500;
  opacity: 0.75;
  color: #eee;
  padding: 4px;
  cursor: pointer;
  transition: color ease-in-out 150ms;

  &:hover {
    color: #eee;
  }
`;
